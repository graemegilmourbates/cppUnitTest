//Evals header file.

template <typename T>
bool ASSERT(T, T);

template <typename T>
bool ASSERT_EQUAL(T, T);

bool ASSERT_ALMOST_EQUAL(std::string, std::string, int);

bool ASSERT_ALMOST_EQUAL(std::string, std::string, int);

template <typename T>
bool ASSERT_ALMOST_EQUAL(T, T, T);
